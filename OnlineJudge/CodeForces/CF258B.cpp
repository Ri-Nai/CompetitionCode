#include<bits/stdc++.h>
using namespace std;
int n,A[20],len,cnt[20];

int dfs(int pos,int now,bool lmt)
{
	if(pos==0)return !now;
	int Up=lmt?A[pos]:9,res=0;
	for(int i=0;s)
}
void solve(int x)
{
	while(x)A[++len]=x%10,x/=10;

}
int main()
{
	scanf("%d",&n);
}